<%= form_for(@photo_gallery_elem) do |f| %>
  <% if @photo_gallery_elem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_gallery_elem.errors.count, "error") %> prohibited this photo_gallery_elem from being saved:</h2>

      <ul>
        <% @photo_gallery_elem.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %> &nbsp;
    <%= f.check_box :display_title %>
    <%= f.label :display_title %>
  </div>
  <div class="field">
    <%= f.label :display_style %><br />
    <%= f.select :display_style, PhotoGalleryElem.display_style_select %>
  </div>

  <%= hidden_field_tag :column_order, @element.column_order %>
  <%= hidden_field_tag :position, @element.position %>
  <%= hidden_field_tag :shortcut, @shortcut %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
