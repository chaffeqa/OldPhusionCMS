<%= render 'show', :locals => {:elem => @photo_gallery_elem} %>

<%= admin_area do %>
  <p>
    | <%= link_to 'Add Photo', new_photo_gallery_elem_photo_path(@photo_gallery_elem) %> |
    <%= link_to 'Gallery Options', edit_photo_gallery_elem_path(@photo_gallery_elem) %> |
    <%= link_to 'Done', shortcut_path(@shortcut) %> |
  </p>
<% end %>
