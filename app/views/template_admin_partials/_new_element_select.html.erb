<%= form_tag(new_element_path(:shortcut => @node.shortcut, :position => locals[:position])) do %>

<%= label_tag :elem_path, "Select an Element" %>
<%= select_tag :elem_path, options_for_select(Element.get_elem_select) %>

<%= submit_tag "Add Element" %>

<% end %>