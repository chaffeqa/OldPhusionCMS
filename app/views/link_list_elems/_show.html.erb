<% elem = locals[:elem] %>

<div class="link-list-elem">
  <h2><%= elem.title %></h2>
  <ul>
    <% elem.links.each do |link| %>
      <li>
        <%= render 'links/link', :locals => {:link => link} %>
      </li>
    <% end %>
  </ul>
</div>
